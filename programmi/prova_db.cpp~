#include <vector>
#include <iostream>
#include "DB_manager.cpp"
#include "immagine.cpp"

using namespace std;

int main() {
	DB_manager manager;
	int i=manager.find_point(1,1,1);
	cout<<i<<endl;
	result R;
	result::const_iterator begin;
	result::const_iterator end;
	manager.load_db(begin, end);
	
	for(result::const_iterator it=begin; it!=end; ++it) {
		int i = it[0].as<int>();
		cout<<i<<endl;
	}
	// = manager.load_db();
	return 0;
}
